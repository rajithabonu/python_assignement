def difference_max_min(arr):
    
    if not arr:
        return 0
    return max(arr) - min(arr)

my_array = [1, 2, 3, 4, 5, 2, 6, 7, 8, 1, 9]
my_array2 = [2,4,6,8,10,12,14]
print(" Difference of max and min:", difference_max_min(my_array))
